import { Router } from 'express';

const router = Router();

// Placeholder — implemented in Module 3
router.get('/', (_req, res) => res.json({ message: 'Stores endpoint — Module 3' }));

export default router;
